#include "student.h"
#include "group.h"

int main() {
	Student st("Ivan", "Ivanov", 14, 16);

	Group group("P33022");

	//for (int i = 0; i < 20; i++) {
	//	group.add(st);
	//}

	//for (int i = 0; i < group.get_size(); i++) {
	//	cout << group.get(i).convert_to_string() << endl;
	//}


	return 0;
}